from core.fasta import leerFasta

def renombrar(listaSeq):
    nuevaLista = []
    
    # Cambiar repetidos
    if listaIDObjetos.count(identificador_limpio[1])-1 != 0:
        identificador_limpio[1] = identificador_limpio[1] + "." + str(listaIDObjetos.count(identificador_limpio[1])-1) 
    # Acceder a una variable de un modulo desde otro

    return nuevaLista

def ignorarDuplicados(listaSeq):
    nuevaLista = []
    
    for seq in listaSeq:
        if listaIDObjetos.count(identificador_limpio[1])-1 == 0:
            nuevaLista.append(seq)
            
    return nuevaLista